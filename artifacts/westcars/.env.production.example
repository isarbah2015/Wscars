# Copy to .env.production (gitignored). Used by scripts/sync-paystack-production-eas.sh
# Get keys from Paystack Dashboard → Settings → API Keys & Webhooks (Live tab).

EXPO_PUBLIC_PAYSTACK_PUBLIC_KEY=pk_live_your_public_key_here

# Server only — set on Firebase, never in the app:
# firebase functions:secrets:set PAYSTACK_SECRET_KEY
# Paste sk_live_...

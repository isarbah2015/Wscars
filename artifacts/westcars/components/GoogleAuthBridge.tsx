/**
 * GoogleAuthBridge — Native (Android / iOS)
 *
 * Metro resolves GoogleAuthBridge.native.tsx on device builds.
 * Client IDs resolve from EXPO_PUBLIC_GOOGLE_* env vars or google-services.json.
 */
export { GoogleAuthBridge } from "./GoogleAuthBridge.native";

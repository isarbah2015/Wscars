---
description: Post-fix commit, push, and EAS build approval workflow for Westcars
alwaysApply: true
---

# Post-Fix Build Approval Workflow

After any fix in this project:

- Commit the fix.
- Push the commit to `main`.
- Stop and ask the developer for confirmation before triggering any EAS build.
- Say exactly: `Ready to trigger EAS build — confirm?`
- Wait for explicit approval.
- Never auto-trigger a build.

After explicit approval, use this build command exactly:

```bash
cd "/Users/apple/Desktop/FUTURE APPS/Wscars/artifacts/westcars" && npx eas build --platform android --profile production --clear-cache
```

The developer monitors builds independently at:
https://expo.dev/accounts/davidsarb/projects/westcars/builds

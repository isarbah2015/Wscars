Complete Prompt to Replit AI – Migrate Westcars to Firebase (with Cloud Functions)

Copy and paste the entire block below directly into Replit AI:

---

```
Migrate the entire Westcars app from Replit backend to Firebase. The app must run completely on Firebase services with no dependency on Replit.

## STEP 1: Remove All Replit Backend Calls

Find and delete/rewrite any code that calls:
- Any URL containing `replit.app`, `localhost`, `127.0.0.1`
- Any `fetch()` or `axios` calls to custom backend endpoints
- Any WebSocket or real‑time connections to Replit

## STEP 2: Replace with Firebase Services

### Firestore (Database)
- Replace all GET/POST/PUT/DELETE requests with Firestore queries:
  - `getDocs(collection(db, 'cars'))`
  - `addDoc(collection(db, 'cars'), newCar)`
  - `updateDoc(doc(db, 'cars', id), updates)`
  - `deleteDoc(doc(db, 'cars', id))`
  - Real‑time listeners with `onSnapshot`

### Authentication
- Already using Firebase Auth – keep as is.

### Storage
- Keep using Firebase Storage for images and ID uploads.

### Cloud Functions (Replace custom backend logic)

Move any server‑side logic from Replit to Firebase Cloud Functions. Create the following functions:

1. **`sendMessageNotification`** – triggers when a new message is added to Firestore; sends push notification to recipient.
2. **`processCarImages`** – when a car listing is created, optimizes/resizes images in Storage.
3. **`calculateCarGenomics`** – triggered on new review; recalculates average ratings and keyword frequencies for that car model.
4. **`sendVerificationEmail`** – sends email when user verifies ID via Smile ID.
5. **`reportContent`** – handles report submissions, auto‑hides content after 3 reports.
6. **`expireListings`** – scheduled function (runs daily) that marks listings older than 30 days as expired.

## STEP 3: Set Up Firebase Project Structure

Create the following folders:

```

firebase/
├── functions/
│   ├── index.js          (all Cloud Functions)
│   ├── package.json
│   └── .eslintrc.js
├── firestore.rules       (security rules)
├── storage.rules         (storage security rules)
└── firebase.json

```

## STEP 4: Update Frontend Code

- Remove any `REPLIT_API_URL` environment variables.
- Update `src/services/firebase.js` to export:
  ```javascript
  import { getFunctions, httpsCallable } from 'firebase/functions';
  export const db = getFirestore(app);
  export const auth = getAuth(app);
  export const storage = getStorage(app);
  export const functions = getFunctions(app);
```

· Replace all API calls with either:
  · Direct Firestore queries (for CRUD)
  · httpsCallable for Cloud Functions (for email, notifications, etc.)

STEP 5: Security Rules

Provide Firestore and Storage security rules that:

· Allow users to read any car listing.
· Allow only the owner to edit/delete their own listings.
· Allow only authenticated users to write reviews and messages.
· Prevent users from setting their own isAdmin or isVerified flags.

STEP 6: Testing & Deployment

After migration:

· Test all features locally using Firebase emulators.
· Deploy functions with firebase deploy --only functions.
· The app must run completely without Replit – only Firebase.

Files to Create/Update

· firebase/functions/index.js
· firebase/functions/package.json
· firebase/firestore.rules
· firebase/storage.rules
· src/services/firebase.js
· src/utils/api.js (if exists – remove or rewrite)
· All screen files that used fetch() or axios (convert to Firestore)

Generate complete, production‑ready code for all of the above. After migration, the app will be fully hosted on Firebase and ready for Play Store submission.

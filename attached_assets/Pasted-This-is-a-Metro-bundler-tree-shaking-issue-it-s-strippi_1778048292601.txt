This is a **Metro bundler tree-shaking issue** — it's stripping keyboard-related code during the production AAB build because it thinks it's unused or web-only.

**Tell Replit exactly this:**

> In `artifacts/westcars`, fix Metro stripping keyboard handling code during the production AAB build. Do all of the following:
>
> **STEP 1 — Check `metro.config.js`**
> Open `metro.config.js` and make sure it is NOT excluding any platform-specific modules. It should look like this:
> ```js
> const { getDefaultConfig } = require('expo/metro-config');
> const path = require('path');
>
> const projectRoot = __dirname;
> const monorepoRoot = path.resolve(projectRoot, '../..');
>
> const config = getDefaultConfig(projectRoot);
>
> config.watchFolders = [monorepoRoot];
>
> config.resolver.nodeModulesPaths = [
>   path.resolve(projectRoot, 'node_modules'),
>   path.resolve(monorepoRoot, 'node_modules'),
> ];
>
> // CRITICAL — never exclude these:
> config.resolver.blockList = [
>   /\.git\/.*/,
>   /.*\/\.local\/.*/,
> ];
>
> // Make sure android platform is fully resolved:
> config.resolver.platforms = ['android', 'ios', 'web', 'native'];
>
> module.exports = config;
> ```
>
> **STEP 2 — Fix `KeyboardAvoidingView` for Production Build**
> In every auth and form screen, replace the current `KeyboardAvoidingView` with this exact production-safe pattern:
> ```tsx
> import {
>   KeyboardAvoidingView,
>   Platform,
>   TouchableWithoutFeedback,
>   Keyboard,
>   ScrollView,
> } from 'react-native';
> import { ScrollView as GHScrollView } from 'react-native-gesture-handler';
>
> <KeyboardAvoidingView
>   style={{ flex: 1 }}
>   behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
>   keyboardVerticalOffset={Platform.OS === 'ios' ? 64 : 0}
> >
>   <TouchableWithoutFeedback onPress={Keyboard.dismiss} accessible={false}>
>     <GHScrollView
>       keyboardShouldPersistTaps="handled"
>       contentContainerStyle={{ flexGrow: 1 }}
>       showsVerticalScrollIndicator={false}
>     >
>       {/* form content */}
>     </GHScrollView>
>   </TouchableWithoutFeedback>
> </KeyboardAvoidingView>
> ```
>
> **STEP 3 — Fix `app.json` to Protect Keyboard Behaviour**
> In `app.json` confirm this is set under `android`:
> ```json
> "android": {
>   "softwareKeyboardLayoutMode": "pan"
> }
> ```
> This is already in your config — just confirm it hasn't been removed. `pan` mode is the most reliable for Android production builds and prevents Metro from optimising it away.
>
> **STEP 4 — Add Keyboard Module to `babel.config.js`**
> Open `babel.config.js` and make sure no transforms are stripping native modules:
> ```js
> module.exports = function (api) {
>   api.cache(true);
>   return {
>     presets: ['babel-preset-expo'],
>     plugins: [
>       // DO NOT add any plugin that strips console or native modules here
>       'react-native-reanimated/plugin', // MUST be last
>     ],
>   };
> };
> ```
> - Remove any `transform-remove-console` plugin — it can strip more than just console calls on aggressive settings
> - Remove any dead code elimination plugins — they misidentify keyboard code as unused on Android
>
> **STEP 5 — Fix `eas.json` Production Profile**
> Make sure the production profile in `eas.json` is NOT using any build flags that strip native modules:
> ```json
> {
>   "build": {
>     "production": {
>       "android": {
>         "buildType": "app-bundle",
>         "gradleCommand": ":app:bundleRelease"
>       },
>       "env": {
>         "EXPO_PUBLIC_FIREBASE_API_KEY": "...",
>         "EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN": "...",
>         "EXPO_PUBLIC_FIREBASE_PROJECT_ID": "...",
>         "EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET": "...",
>         "EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID": "...",
>         "EXPO_PUBLIC_FIREBASE_APP_ID": "..."
>       }
>     }
>   }
> }
> ```
> - Make sure `shrinkResources` is NOT set to `true` — this strips keyboard and input resources on Android
> - Make sure `minifyEnabled` is NOT overridden to `true` in any custom Gradle config
>
> **STEP 6 — Audit Every Form Screen**
> Find every screen with a `TextInput` and confirm all of them have:
> - `ScrollView` from `react-native-gesture-handler` ✅
> - `keyboardShouldPersistTaps="handled"` ✅
> - `KeyboardAvoidingView` with correct `behavior` per platform ✅
> - `TouchableWithoutFeedback` with `Keyboard.dismiss` for tapping outside ✅
> - No `pointerEvents="none"` on any parent View that would block input
> - No absolute positioned overlay sitting above TextInput fields
>
> **STEP 7 — Clean and Rebuild**
> ```bash
> rm -rf .expo
> rm -rf node_modules/.cache
> rm -rf /tmp/metro-*
> rm -rf /tmp/haste-*
> pnpm exec expo start --clear
> ```
> Then run TypeScript:
> ```bash
> pnpm exec tsc --noEmit
> ```
>
> **Report back with:**
> - Current contents of `metro.config.js`
> - Current contents of `babel.config.js`
> - Current contents of `eas.json`
> - Every form screen that was updated
> - TypeScript result
> - Confirmation `softwareKeyboardLayoutMode: pan` is still in `app.json`
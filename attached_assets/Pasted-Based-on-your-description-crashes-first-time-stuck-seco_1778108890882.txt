Based on your description (crashes first time, stuck second time), these are the likely issues:

Symptom Likely Cause
Crashes on first open Native module mismatch or Firebase config error
Stuck on second open AsyncStorage corruption or navigation loop

---

✅ Complete Fix – Copy and Paste These Commands in Order

STEP 1: Full Clean in Replit Shell

```bash
# Remove everything that could cause conflicts
rm -rf node_modules .expo android ios package-lock.json .expo-shared
npm cache clean --force
rm -rf $TMPDIR/metro-cache
rm -rf $TMPDIR/haste-map-metro-*
```

STEP 2: Reinstall with Exact Versions (No Conflicts)

```bash
# Fresh install
npm install

# Ensure Firebase versions are compatible
npm install firebase@10.12.0 @react-native-firebase/app@19.2.0 @react-native-firebase/auth@19.2.0 @react-native-firebase/firestore@19.2.0 @react-native-firebase/storage@19.2.0
```

STEP 3: Verify Firebase Config (CRITICAL)

Open src/services/firebase.js and confirm it looks EXACTLY like this (with YOUR real keys):

```javascript
import { initializeApp } from 'firebase/app';
import { getAuth, initializeAuth, getReactNativePersistence } from 'firebase/auth';
import { getFirestore } from 'firebase/firestore';
import { getStorage } from 'firebase/storage';
import AsyncStorage from '@react-native-async-storage/async-storage';

// 🔴 REPLACE WITH YOUR REAL FIREBASE CONFIG
const firebaseConfig = {
  apiKey: "AIzaSyD_xxxxxxxxxxxxx",  // Must be REAL
  authDomain: "westcars-ghana.firebaseapp.com",
  projectId: "westcars-ghana",
  storageBucket: "westcars-ghana.firebasestorage.app",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:abc123def456"
};

const app = initializeApp(firebaseConfig);

// ✅ Correct persistence setup (fixes AsyncStorage crash)
export const auth = initializeAuth(app, {
  persistence: getReactNativePersistence(AsyncStorage)
});

export const db = getFirestore(app);
export const storage = getStorage(app);
```

Common mistakes that cause crashes:

· ❌ Placeholder values like "YOUR_API_KEY"
· ❌ Missing initializeAuth with persistence
· ❌ Wrong project ID

STEP 4: Clean app.json

Make sure your app.json has NO conflicting plugins:

```json
{
  "expo": {
    "name": "Westcars",
    "slug": "westcars",
    "version": "1.0.0",
    "orientation": "portrait",
    "icon": "./assets/icon.png",
    "userInterfaceStyle": "automatic",
    "splash": {
      "image": "./assets/splash.png",
      "resizeMode": "contain",
      "backgroundColor": "#FF6B00"
    },
    "assetBundlePatterns": ["**/*"],
    "ios": {
      "supportsTablet": true,
      "bundleIdentifier": "com.westcars.app"
    },
    "android": {
      "adaptiveIcon": {
        "foregroundImage": "./assets/adaptive-icon.png",
        "backgroundColor": "#FF6B00"
      },
      "package": "com.westcars.app",
      "permissions": ["INTERNET", "CAMERA", "READ_EXTERNAL_STORAGE"]
    },
    "plugins": [
      "expo-image-picker",
      "expo-location"
    ],
    "extra": {
      "eas": {
        "projectId": "YOUR_EAS_PROJECT_ID"
      }
    }
  }
}
```

Remove these if present:

· react-native-keyboard-controller (unless you fixed it properly)
· Any other native module without proper setup

STEP 5: Create or Update eas.json

```json
{
  "cli": {
    "version": ">= 7.0.0"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal"
    },
    "preview": {
      "android": {
        "buildType": "apk"
      }
    },
    "production": {
      "android": {
        "buildType": "app-bundle"
      }
    }
  },
  "submit": {
    "production": {}
  }
}
```

STEP 6: Clear Metro Bundler Cache and Prebuild

```bash
npx expo prebuild --clean
npx expo start --clear
```

STEP 7: Build Fresh APK

```bash
eas build --platform android --profile preview --clear-cache
```

Wait for build to complete (10-15 minutes).

---

📋 Checklist Before Building

Check Command/File Expected
Firebase config has real keys `cat src/services/firebase.js grep apiKey`
No keyboard-controller in app.json `cat app.json grep keyboard`
Correct EAS project ID `cat app.json grep projectId`
All dependencies installed npm list --depth=0 No missing packages

---

🔧 If Still Crashing After Build

Run this diagnostic to see the ACTUAL error:

```bash
# Install Android Debug Bridge (if not installed)
# Connect your phone via USB with debugging enabled
adb logcat | grep -E "AndroidRuntime|FATAL|Westcars"
```

Copy the red error lines and paste them here.

---

📋 Complete Summary – What You Just Fixed

Problem Solution
Native module mismatch Removed node_modules and prebuilt folders
Firebase persistence crash Fixed initializeAuth with AsyncStorage
Version conflicts Installed compatible Firebase versions
Corrupt cache Cleared all caches
Bad build Rebuilt with --clear-cache

---

✅ Run This One Command to Do Everything

If you want a single command that handles steps 1-6:

```bash
rm -rf node_modules .expo android ios package-lock.json && npm cache clean --force && npm install && npx expo prebuild --clean && eas build --platform android --profile preview --clear-cache
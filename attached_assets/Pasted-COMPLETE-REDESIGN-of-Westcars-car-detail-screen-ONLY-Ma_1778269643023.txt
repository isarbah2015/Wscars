COMPLETE REDESIGN of Westcars car detail screen ONLY. Match the design style from the attached reference image (icons, buttons, layout). Keep ALL existing functionality and ALL existing information (mileage, fuel type, transmission, features, seller info, description). DO NOT change the home screen 2x2 grid or its colors.

## CRITICAL FIXES (Must Work Alongside Redesign)

### FIX A: Google Sign-In Not Working
Rewrite Google Sign-In in welcome/login screen:
- Import GoogleAuthProvider, signInWithRedirect from firebase/auth
- Create handleGoogleSignIn function with try/catch
- Use signInWithRedirect(auth, provider) for mobile
- Add error alert if fails

### FIX B: Create Account / Sign-Up Not Working
Rewrite sign-up handler:
- Use createUserWithEmailAndPassword
- Save user data to Firestore users collection (name, email, phone, createdAt, isVerified: false, trustScore: 0)
- Navigate to home on success
- Show error alert if fails

### FIX C: "Browse as Guest" Button
Add guest mode button that navigates directly to home without authentication

### FIX D: Sign-In Pages Colors & Arrows
- Match auth screen colors to main page colors
- Remove ALL arrow icons from login/signup cards
- Use clean rounded buttons with gradient fill

## CAR DETAIL SCREEN REDESIGN (Based on Reference Image)

Target file: app/car/[id].tsx

### Layout Structure (Match Reference Image Style):

**Header:**
- Back arrow button (top left)
- Share button (top right)
- Favorite/heart button (top right, next to share)
- Clean, minimal design

**Image Gallery:**
- Full width car image at top
- Small dot indicators showing current image
- "View all photos" button or counter (e.g., "1/8")
- Image height: approximately 280-320px

**Title & Price Section:**
- Car brand + model (e.g., "Toyota Camry")
- Year badge next to title
- Price in GHS (large, bold, accent color)
- Original price comparison (if applicable)

**Key Specs Row (Horizontal icons + text):**
Using icons similar to reference image:
- 📊 Mileage (e.g., "45,000 km")
- ⛽ Fuel Type (e.g., "Petrol")
- 🔧 Transmission (e.g., "Automatic")
- 📅 Year (e.g., "2021")

**Location:**
- Pin icon + location (e.g., "Accra, Ghana")

**Key Features / Equipment Section:**
- Collapsible categories matching reference image style
- Category headers with icon + title + item count + chevron
- Example categories: Safety, Comfort, Interior, Multimedia, Visibility, Exterior, Anti-theft, Other
- Expand/collapse animation
- Features displayed in grid or list with checkmark icons

**Seller Info Card:**
- Seller avatar/icon
- Seller name
- Verified badge (if applicable)
- Member since date
- Rating stars with review count
- Trust score
- Contact buttons: Call, WhatsApp, Message (rounded, modern)

**Description Section:**
- Expandable text with "Read more" button
- Key highlights as chips (e.g., "1 owner", "Full service history", "Negotiable")

**Similar Listings Section:**
- Horizontal scroll of related cars
- Each card shows image, price, title

### Icons to Use (from reference image style):
- Use clean, outline-style icons
- Consistent size (20-24px)
- Accent color for interactive icons
- Secondary color for info icons

### Buttons Design (from reference image style):
- Rounded corners (30px)
- Gradient or solid fill for primary buttons
- Outline for secondary buttons
- Touch feedback on press

### Color Palette:
- USE THE EXACT SAME COLORS FROM YOUR CURRENT MAIN PAGE (home screen 2x2 grid)
- Extract accent color from main page
- Extract text colors from main page
- Extract background colors from main page
- DO NOT invent new colors

## WHAT TO KEEP (DO NOT CHANGE):

- Home screen 2x2 grid - LEAVE EXACTLY AS IS
- app/(tabs)/home.tsx - DO NOT MODIFY
- ALL colors on main page - DO NOT CHANGE
- All car data (mileage, fuel, transmission, features, seller info, description) - MUST REMAIN
- Firebase data fetching logic
- Navigation structure
- Favorite button functionality
- Contact buttons functionality (call, WhatsApp, message)

## FILES TO UPDATE:

- app/welcome.tsx or app/index.tsx (auth fixes)
- app/auth/login.tsx (auth fixes)
- app/auth/signup.tsx (auth fixes)
- app/car/[id].tsx (COMPLETE REDESIGN matching reference image style)

## FILES TO LEAVE EXACTLY AS IS (DO NOT TOUCH):

- app/(tabs)/home.tsx - KEEP 2x2 GRID AND COLORS UNCHANGED
- app/(tabs)/search.tsx
- app/(tabs)/profile.tsx
- Any component affecting the main page grid or colors

## CRITICAL INSTRUCTIONS:

1. The car detail screen must match the reference image design style (icons, buttons, layout)
2. Keep EVERY piece of existing information (mileage, fuel, transmission, features, seller info, description)
3. Only change the VISUAL DESIGN of the car detail screen
4. Home screen 2x2 grid must look EXACTLY THE SAME as before
5. Use the same colors from your main page on the car detail screen

Generate complete, working code for all affected files. Authentication MUST work. Home screen MUST stay exactly the same. Car detail screen MUST have premium design matching reference image with all existing information preserved.
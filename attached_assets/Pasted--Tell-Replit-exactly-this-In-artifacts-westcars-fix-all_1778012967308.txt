**Tell Replit exactly this:**

> In `artifacts/westcars`, fix all non-functional clickable cards and do a complete professional UI polish across the entire app. Do all of the following:
>
> **STEP 1 — Fix All Non-Clickable Cards**
> - Find every card component across the entire app that should be tappable (listing cards, seller cards, category cards, vehicle cards, feature cards, etc.)
> - Replace any `<View>` that should be tappable with `<TouchableOpacity>` or `<Pressable>`:
> ```tsx
> // WRONG — View is never tappable:
> <View onPress={...}>
>
> // CORRECT:
> <TouchableOpacity
>   activeOpacity={0.7}
>   onPress={() => router.push(`/listing/${item.id}`)}
> >
> ```
> - Check every card that has an `onPress` prop — if the parent is a `View`, the press will silently fail on native
> - Find all `FlatList` and `ScrollView` rendered items — every item that should navigate must be wrapped in `TouchableOpacity` not `View`
> - If any card is inside a `ScrollView`, make sure the `ScrollView` has `keyboardShouldPersistTaps="handled"` so taps aren't swallowed
>
> **STEP 2 — Fix Gesture Handler Conflicts on Cards**
> - Any card inside a `GestureDetector` or `PanGestureHandler` (e.g. swipeable cards) must use `TouchableOpacity` from `react-native-gesture-handler` not `react-native`:
> ```tsx
> import { TouchableOpacity } from 'react-native-gesture-handler';
> ```
> - Any card inside a `FlatList` that is itself inside a `ScrollView` — add `scrollEnabled={false}` to the inner FlatList and `nestedScrollEnabled={true}` to fix touch conflict
> - Add `pointerEvents="box-none"` to any decorative overlay Views sitting on top of cards that might be stealing touches
>
> **STEP 3 — Audit Every Navigation Action**
> - Find every card `onPress` and verify it actually calls `router.push()` or `router.navigate()` with a valid route
> - Find any `onPress={() => {}}` empty handlers and implement the correct navigation
> - Find any `onPress={undefined}` and either implement it or remove the touchable wrapper
> - Verify every route string matches an actual file in the `app/` directory — broken routes fail silently on native
> - Add error boundaries around navigation calls:
> ```tsx
> onPress={() => {
>   try {
>     router.push(`/listing/${item.id}`);
>   } catch (e) {
>     console.error('Navigation failed:', e);
>   }
> }}
> ```
>
> **STEP 4 — Seller Profile Screen Full Fix**
> - Fix phone number — fully inside card, no overflow, `numberOfLines={1}` `adjustsFontSizeToFit`
> - Fix ID verification — perfectly centered using `alignItems: 'center'` `justifyContent: 'center'`
> - Fix all listing cards under seller profile — must be tappable and navigate to listing detail
> - Profile action buttons (call, message, share) — must all be functional with proper `onPress` handlers
> - If call button exists: `onPress={() => Linking.openURL('tel:+PHONENUMBER')}`
> - If message button exists: `onPress={() => Linking.openURL('sms:+PHONENUMBER')}`
>
> **STEP 5 — Global Card Component Standards**
> Apply to EVERY card across the entire app:
> ```tsx
> <TouchableOpacity
>   activeOpacity={0.7}
>   onPress={onPress}
>   style={{
>     backgroundColor: '#FFFFFF',
>     borderRadius: 12,
>     padding: 16,
>     marginHorizontal: 16,
>     marginVertical: 8,
>     shadowColor: '#000',
>     shadowOffset: { width: 0, height: 2 },
>     shadowOpacity: 0.08,
>     shadowRadius: 8,
>     elevation: 3,
>     overflow: 'hidden',
>   }}
> >
> ```
> - All card titles: `fontSize: 16, fontWeight: '600'`
> - All card body text: `fontSize: 14, fontWeight: '400'`
> - All card secondary text: `fontSize: 12, color: '#666'`
> - All icons inside cards: `flexDirection: 'row', alignItems: 'center', gap: 8`
>
> **STEP 6 — Typography, Spacing and Design Polish**
> - No raw `<Text>` without explicit `fontSize`, `color`, `fontFamily`
> - All spacing in multiples of 4 (4, 8, 12, 16, 20, 24)
> - All headings consistent size across same-level screens
> - All body text consistent size and color
> - `lineHeight` set on all multi-line text: `lineHeight: fontSize * 1.5`
> - All `TouchableOpacity` must have `activeOpacity={0.7}`
> - All loading states: `ActivityIndicator` centered in container
> - All error states: user-friendly message — no raw error strings
> - All empty states: centered illustration or message — no blank screens
> - All images: `placeholder` or `defaultSource` — no broken image icons
> - All prices: same currency format everywhere
> - All dates: same date format everywhere
> - Brand colors `#0A1628` consistent throughout
>
> **STEP 7 — Final Verification**
> - Run `tsc --noEmit` — must exit clean
> - List every file changed with what was broken and what was fixed
> - Confirm every card that should be tappable now has `TouchableOpacity` or `Pressable` as its root
> - Confirm no `onPress` handlers are empty or undefined
> - Confirm no `undefined` or `null` values are being rendered as text
> - Confirm Metro logs show zero layout or navigation warnings
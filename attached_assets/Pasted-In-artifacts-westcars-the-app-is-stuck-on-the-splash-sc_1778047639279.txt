In artifacts/westcars, the app is stuck on the splash screen forever on Android — the WC logo shows but the app never loads past it. SplashScreen.hideAsync() is never being called because fonts or Firebase are hanging. Fix all of the following:
STEP 1 — Add a Splash Screen Timeout Safety Net
In app/_layout.tsx, add a hard timeout so the app ALWAYS moves past the splash even if fonts or Firebase hang:
tsxuseEffect(() => {
  // Safety net — never stay on splash more than 5 seconds
  const timeout = setTimeout(async () => {
    try {
      await SplashScreen.hideAsync();
    } catch (e) {}
  }, 5000);
  return () => clearTimeout(timeout);
}, []);
STEP 2 — Fix Font Loading Hanging Forever
Replace the current font loading logic with this exact pattern that handles errors and never hangs:
tsxconst [fontsLoaded, fontError] = useFonts({
  ...Ionicons.font,
  ...Feather.font,
  ...MaterialIcons.font,
  ...MaterialCommunityIcons.font,
  ...FontAwesome.font,
  ...FontAwesome5.font,
  ...AntDesign.font,
  ...Entypo.font,
  // all custom fonts
});

useEffect(() => {
  // Hide splash as soon as fonts load OR error — never wait forever
  if (fontsLoaded || fontError) {
    SplashScreen.hideAsync().catch(() => {});
  }
}, [fontsLoaded, fontError]);

// Never block render waiting for fonts
if (!fontsLoaded && !fontError) {
  return null;
}
STEP 3 — Fix Firebase Blocking App Load
Firebase initialization must NEVER block the app from rendering. Find where Firebase is initialized and wrap it so it fails gracefully:
tsx// In your firebase config file, wrap initializeApp in try/catch:
let app;
try {
  app = getApps().length === 0 ? initializeApp(firebaseConfig) : getApps()[0];
} catch (e) {
  console.error('Firebase init failed:', e);
}

Firebase must initialize in the background — never await it before rendering the app
Remove any await on Firebase calls at the top level of _layout.tsx
If Firebase auth state listener is in _layout.tsx, wrap it in try/catch and make sure it never blocks the initial render

STEP 4 — Fix the Exact Splash Hide Logic
Find the current SplashScreen.hideAsync() call and make sure it follows this exact flow:
tsx// AT MODULE LEVEL — before any component:
SplashScreen.preventAutoHideAsync();

// INSIDE ROOT COMPONENT:
export default function RootLayout() {
  const [fontsLoaded, fontError] = useFonts({ ... });
  const [appReady, setAppReady] = useState(false);

  useEffect(() => {
    async function prepare() {
      try {
        // any async prep here — but with a timeout
        await Promise.race([
          initializeAppServices(), // your firebase etc
          new Promise(resolve => setTimeout(resolve, 3000)) // 3s max
        ]);
      } catch (e) {
        console.error('App prepare failed:', e);
      } finally {
        setAppReady(true); // ALWAYS set ready
      }
    }
    prepare();
  }, []);

  useEffect(() => {
    if ((fontsLoaded || fontError) && appReady) {
      SplashScreen.hideAsync().catch(() => {});
    }
  }, [fontsLoaded, fontError, appReady]);

  if ((!fontsLoaded && !fontError) || !appReady) {
    return null;
  }

  return (
    // your navigation stack
  );
}
STEP 5 — Check for Infinite Loops

Find any useEffect in _layout.tsx that has missing or wrong dependencies — these cause infinite re-renders that block the app
Find any auth state listener that re-triggers on every render — wrap with useRef guard:

tsxconst listenerSet = useRef(false);
useEffect(() => {
  if (listenerSet.current) return;
  listenerSet.current = true;
  const unsub = onAuthStateChanged(auth, (user) => {
    setUser(user);
  });
  return unsub;
}, []);
STEP 6 — Clear All Cache and Restart
bashrm -rf .expo
rm -rf node_modules/.cache
rm -rf /tmp/metro-*
rm -rf /tmp/haste-*
pnpm exec expo start --clear
STEP 7 — Verify

Confirm SplashScreen.preventAutoHideAsync() is at module level, outside the component
Confirm SplashScreen.hideAsync() is called when fontsLoaded || fontError AND appReady are both true
Confirm Firebase never blocks the initial render
Confirm the 5 second safety net timeout is in place
Confirm no top-level await exists outside of async functions
Run tsc --noEmit — must exit clean
Report every file changed and exactly what was causing the hang
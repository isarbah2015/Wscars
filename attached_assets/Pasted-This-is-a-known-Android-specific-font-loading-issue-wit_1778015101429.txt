This is a **known Android-specific font loading issue** with `@expo/vector-icons` on Expo Go. iOS and web handle font fallbacks automatically, Android doesn't.

**Tell Replit exactly this:**

> In `artifacts/westcars`, fix vector icons showing on iOS and web but not on Android in Expo Go. This is an Android font loading timing issue. Fix all of the following:
>
> **STEP 1 — Force Load All Icon Fonts Explicitly**
> In `app/_layout.tsx`, update the `useFonts` call to explicitly include every icon font family used anywhere in the app:
> ```tsx
> import { useFonts } from 'expo-font';
> import { Ionicons } from '@expo/vector-icons';
> import { Feather } from '@expo/vector-icons';
> import { MaterialIcons } from '@expo/vector-icons';
> import { MaterialCommunityIcons } from '@expo/vector-icons';
> import { FontAwesome } from '@expo/vector-icons';
> import { FontAwesome5 } from '@expo/vector-icons';
> import { AntDesign } from '@expo/vector-icons';
> import { Entypo } from '@expo/vector-icons';
>
> const [fontsLoaded, fontError] = useFonts({
>   ...Ionicons.font,
>   ...Feather.font,
>   ...MaterialIcons.font,
>   ...MaterialCommunityIcons.font,
>   ...FontAwesome.font,
>   ...FontAwesome5.font,
>   ...AntDesign.font,
>   ...Entypo.font,
>   // your custom fonts below:
>   'Manrope': require('../assets/fonts/Manrope.ttf'),
>   'PlusJakartaSans': require('../assets/fonts/PlusJakartaSans.ttf'),
>   // add any other custom fonts here
> });
> ```
> - First scan the entire codebase and find EVERY icon family imported from `@expo/vector-icons` — add all of them to this list
> - Do NOT render anything until `fontsLoaded || fontError` is true
> - Keep `SplashScreen.hideAsync()` strictly gated after fonts resolve
>
> **STEP 2 — Fix Android Font Resolution**
> - In `app/_layout.tsx` add this Android-specific font fix before the return:
> ```tsx
> import { Platform } from 'react-native';
>
> // Force Android to wait for fonts
> if (Platform.OS === 'android' && !fontsLoaded && !fontError) {
>   return null; // keep splash visible
> }
> ```
> - Check `babel.config.js` — make sure it has `reanimated` plugin LAST:
> ```js
> module.exports = {
>   presets: ['babel-preset-expo'],
>   plugins: [
>     // other plugins first
>     'react-native-reanimated/plugin', // MUST BE LAST
>   ],
> };
> ```
>
> **STEP 3 — Fix Icon Components for Android**
> - Search entire codebase for any icon rendered as:
>   - Raw unicode characters e.g. `★` `→` `☰` — replace with proper `<Ionicons>` component
>   - Emoji used as icons e.g. `🚗` `📍` — replace with proper icon component
>   - SVG icons that don't have Android-compatible props
> - For every icon component, make sure `size` and `color` props are always explicitly set:
> ```tsx
> // WRONG — missing props cause Android render failure:
> <Ionicons name="car" />
>
> // CORRECT:
> <Ionicons name="car" size={24} color="#0A1628" />
> ```
> - Replace any icon using `style` prop for color — Android ignores styled color on icon fonts:
> ```tsx
> // WRONG on Android:
> <Ionicons name="car" style={{ color: '#0A1628' }} />
>
> // CORRECT:
> <Ionicons name="car" size={24} color="#0A1628" />
> ```
>
> **STEP 4 — Clear Android Font Cache**
> ```bash
> rm -rf .expo
> rm -rf node_modules/.cache
> rm -rf /tmp/metro-*
> rm -rf /tmp/haste-*
> pnpm exec expo start --clear
> ```
>
> **STEP 5 — Verify**
> - Scan entire codebase and list every icon family used from `@expo/vector-icons`
> - Confirm all of them are included in `useFonts` in `_layout.tsx`
> - Confirm `reanimated/plugin` is last in `babel.config.js`
> - Confirm no icon uses `style` prop for color instead of `color` prop
> - Confirm no raw unicode or emoji used as UI icons
> - Run `tsc --noEmit` — must exit clean
> - Report every file changed and what was fixed
Completely redesign the Car Detail Screen to have a PREMIUM, MODERN, LUXURY look while keeping ALL existing features (price comparison, 8 equipment categories, seller info, chat buttons, favorites, etc.).

## DESIGN REQUIREMENTS:

### 1. Hero Image Section
- Full-width car image gallery at top (parallax scroll effect)
- Gradient overlay from bottom (dark to transparent) for better text visibility
- Back button (top left) – circular with blur background
- Share button (top right) – circular with blur background
- Favorite button (next to share) – circular with blur background
- Current image counter (e.g., "1/8") – pill shape at bottom right of image

### 2. Price & Title Section (Sticky on scroll)
- Car brand + model in large bold font (28px)
- Year badge (pill shape) next to title
- Price in large font (32px) with GHS currency and orange color
- Monthly estimate (optional small text: "Estimated GHS 2,500/month")

### 3. Key Stats Row (Horizontal scroll or grid)
Display 4 key stats in glassmorphic cards:
- Mileage (with speedometer icon)
- Fuel type (with gas pump icon)
- Transmission (with gear icon)
- Condition (with star or check icon)

### 4. Price Comparison Alert (already added)
- Keep existing price warning feature

### 5. Seller Info Card (Premium design)
- Horizontal layout with seller avatar (large, with verification badge)
- Seller name and member since
- Trust score with progress ring or star rating
- "View Profile" button
- Contact buttons: Call, WhatsApp, Message (gradient buttons with icons)

### 6. Equipment Categories (already added)
- Keep the 8 collapsible categories
- Redesign category headers with gradient backgrounds
- Add icon for each category (e.g., shield for safety, seat for comfort)

### 7. Description Section
- Expandable text with "Read more" button
- Key highlights as chips (e.g., "1 owner", "Full service history", " negotiable")

### 8. Location Section
- Map preview (static image or actual map)
- Address with pin icon
- "Get Directions" button

### 9. Similar Listings Section
- Horizontal scroll of related cars (same brand or similar price)
- Each card shows image, price, short title

### 10. Bottom Action Bar (Fixed)
- Floating bar at bottom with two buttons:
  - Message Seller (gradient orange, full width on left)
  - Call (outline with phone icon, on right)
- Sticky on scroll, appears after hero image passes

## VISUAL STYLE:

- **Colors:** Primary #FF6B00, Secondary #1F2937, Background #F8FAFC
- **Cards:** Glassmorphic with 16px border radius, subtle shadows, white/transparent backgrounds
- **Typography:** SF Pro or Inter font, clean and modern
- **Animations:** Smooth fade-in for sections as they scroll into view
- **Spacing:** Generous padding (20px) between sections

## TECHNICAL REQUIREMENTS:

- Use `react-native-reanimated` for parallax header effect
- Use `react-native-fast-image` for optimized image loading
- Keep ALL existing functionality (Firebase, chat, favorites, price comparison, equipment categories)
- Maintain dark/light mode support
- Ensure smooth scrolling performance

## FILES TO UPDATE:

- `src/screens/CarDetailScreen.js` – Complete redesign

Generate the complete updated code with all existing features preserved and the new premium design implemented.
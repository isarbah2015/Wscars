Give Replit AI this prompt:

---

**Find and fix the exact reason TextInputs are flickering, losing focus, and keyboard dismisses instantly on Sign In, Sign Up and Create Account screens. Do a full diagnosis first, then fix.**

**Step 1 — Diagnose, search these exact patterns in the auth screen files:**

- Any `useState` or `setState` call inside a `useEffect` that runs on every render — this causes infinite re-render loop which kills keyboard
- Any parent component wrapping auth screens that re-renders on every keystroke (e.g. state defined in a parent that updates on input change)
- Any `NavigationContainer` or `Stack.Navigator` re-mounting the screen while typing
- Any `Animated` value updating on every render near the auth screens
- Any `KeyboardAvoidingView` nested inside another `KeyboardAvoidingView`
- Any `ScrollView` without `keyboardShouldPersistTaps="handled"`
- Any `TouchableWithoutFeedback` or `Pressable` on the screen wrapper calling `Keyboard.dismiss()`
- Any `GestureHandlerRootView` placed INSIDE a screen instead of at the app root

**Step 2 — Fix all auth screen TextInputs:**

```jsx
<TextInput
  value={value}
  onChangeText={setValue}
  autoCorrect={false}
  autoCapitalize="none"
  blurOnSubmit={false}
  pointerEvents="auto"
  underlineColorAndroid="transparent"
/>
```

**Step 3 — Fix the screen wrapper on every auth screen:**

```jsx
<KeyboardAvoidingView
  style={{ flex: 1 }}
  behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
>
  <ScrollView
    keyboardShouldPersistTaps="handled"
    contentContainerStyle={{ flexGrow: 1 }}
  >
    <TouchableWithoutFeedback onPress={Keyboard.dismiss}>
      <View style={{ flex: 1 }}>
        {/* all form content here */}
      </View>
    </TouchableWithoutFeedback>
  </ScrollView>
</KeyboardAvoidingView>
```

**Step 4 — Fix the re-render loop:**

- Move every `useState` for form fields (email, password, name etc.) to the TOP of the auth screen component, never inside a condition or nested component
- If form state is stored in a Context or parent — move it LOCAL to the auth screen component only
- Wrap the entire auth screen component export with `React.memo()` to prevent unnecessary re-renders:
```jsx
export default React.memo(SignInScreen);
```

**Step 5 — Fix app root `_layout.tsx`:**

```jsx
import { GestureHandlerRootView } from 'react-native-gesture-handler';

export default function RootLayout() {
  return (
    <GestureHandlerRootView style={{ flex: 1 }}>
      <AppContextProvider>
        <Stack screenOptions={{ headerShown: false }} />
      </AppContextProvider>
    </GestureHandlerRootView>
  );
}
```

**Do not change any Firebase logic, navigation routes, design/styling, or any working screens — only fix the auth screen input behaviour.**

---

This covers every single possible cause — the diagnosis step will find the exact one!
Fix all the following issues in one go. Do not break any existing functionality, navigation, app structure or styling:
Fix 1 — TextInputs flickering, losing focus and keyboard dismissing on Sign In, Sign Up and Create Account screens:

Search for any useState or setState inside a useEffect that runs on every render — causes infinite re-render loop which kills keyboard
Search for any parent component wrapping auth screens that re-renders on every keystroke
Search for any KeyboardAvoidingView nested inside another KeyboardAvoidingView
Search for any ScrollView without keyboardShouldPersistTaps="handled"
Search for any TouchableWithoutFeedback or Pressable on the screen wrapper calling Keyboard.dismiss()
Search for any GestureHandlerRootView placed INSIDE a screen instead of at the app root

Fix every auth screen TextInput with these props:
jsx<TextInput
  value={value}
  onChangeText={setValue}
  autoCorrect={false}
  autoCapitalize="none"
  blurOnSubmit={false}
  pointerEvents="auto"
  underlineColorAndroid="transparent"
/>
Fix every auth screen wrapper:
jsx<KeyboardAvoidingView
  style={{ flex: 1 }}
  behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
>
  <ScrollView
    keyboardShouldPersistTaps="handled"
    contentContainerStyle={{ flexGrow: 1 }}
  >
    <TouchableWithoutFeedback onPress={Keyboard.dismiss}>
      <View style={{ flex: 1 }}>
        {/* all form content here */}
      </View>
    </TouchableWithoutFeedback>
  </ScrollView>
</KeyboardAvoidingView>
Fix re-render loop — move all form field useState to the TOP of each auth screen component and wrap with:
jsxexport default React.memo(SignInScreen);
Fix app root _layout.tsx:
jsximport { GestureHandlerRootView } from 'react-native-gesture-handler';

export default function RootLayout() {
  return (
    <GestureHandlerRootView style={{ flex: 1 }}>
      <AppContextProvider>
        <Stack screenOptions={{ headerShown: false }} />
      </AppContextProvider>
    </GestureHandlerRootView>
  );
}

Fix 2 — Remove WC logo appearing before splash screen:

Find the app.json or app.config.js and ensure the splash screen config is set correctly:

json"splash": {
  "image": "./assets/splash.png",
  "resizeMode": "contain",
  "backgroundColor": "#ffffff"
}

In _layout.tsx, use expo-splash-screen to hide the splash only after app is fully ready:

jsximport * as SplashScreen from 'expo-splash-screen';

SplashScreen.preventAutoHideAsync();

export default function RootLayout() {
  const [appReady, setAppReady] = useState(false);

  useEffect(() => {
    async function prepare() {
      try {
        // load fonts, firebase init, etc here
      } finally {
        setAppReady(true);
        await SplashScreen.hideAsync();
      }
    }
    prepare();
  }, []);

  if (!appReady) return null;

  return (
    <GestureHandlerRootView style={{ flex: 1 }}>
      <AppContextProvider>
        <Stack screenOptions={{ headerShown: false }} />
      </AppContextProvider>
    </GestureHandlerRootView>
  );
}

Remove any manual logo/splash component that renders the WC logo before navigation loads
Search for any SplashScreen component file or LogoScreen and remove it from the navigation stack entirely
Do NOT delete the actual splash image asset


Do not change any Firebase logic, API calls, data models, navigation routes, UI design or any working screens — only fix the above two issues.
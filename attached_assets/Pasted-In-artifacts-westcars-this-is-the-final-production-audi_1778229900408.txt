In artifacts/westcars, this is the final production audit before Google Play Store. Do NOT report anything as done or fixed unless you have physically verified it by reading the actual file contents and pasting proof. Do not assume — verify everything. Zero mistakes allowed.
RULE: Before marking any step complete you must:

Read the actual file and paste the relevant lines confirming the fix is in place
Run every command and paste the actual output
If anything fails do not move on — fix it first then verify again
Never say "this should work" or "this is fixed" without showing proof

STEP 0 — Expo Account Cleanup and Switch to lyon7sarbah

Check current logged in account:

basheas whoami
npx expo whoami

Log out of everything completely:

basheas logout
npx expo logout

Remove any davidsarb references everywhere:

bashgrep -rn "davidsarb" artifacts/westcars --include="*.json" --include="*.ts" --include="*.tsx"

Paste output — fix every result found, replacing davidsarb with lyon7sarbah
Update app.json owner to exactly "lyon7sarbah":

bashgrep "owner" artifacts/westcars/app.json

Must return exactly "owner": "lyon7sarbah" — nothing else
Set the Expo token for lyon7sarbah account:

bashexport EXPO_TOKEN=<token from Replit secrets for lyon7sarbah>
eas whoami

Must return lyon7sarbah before proceeding
Confirm EAS project is under lyon7sarbah:

bashcd artifacts/westcars
eas init --id e61e8d7b-541e-491c-afa1-219ca6f64d2d --force

Confirm app.json extra.eas.projectId is e61e8d7b-541e-491c-afa1-219ca6f64d2d
Confirm eas.json production profile is correctly configured
PROOF REQUIRED: Paste actual output of eas whoami and owner line from app.json

STEP 1 — Full Clean
Run and paste actual output:
bashrm -rf .expo
rm -rf node_modules/.cache
rm -rf /tmp/metro-*
rm -rf /tmp/haste-*
find . -name "*.orig" -delete
find . -name ".DS_Store" -delete
find . -name "*.log" -delete
pnpm store prune
pnpm dedupe

PROOF REQUIRED: Paste actual terminal output confirming each deletion

STEP 2 — Remove ALL Unnecessary Files

Find and delete every file not referenced anywhere:

bashgrep -rn "davidsarb" artifacts/westcars --include="*.json" --include="*.ts" --include="*.tsx" --include="*.js"

Delete any .bak, .old, .copy, .temp files:

bashfind artifacts/westcars -name "*.bak" -o -name "*.old" -o -name "*.copy" -o -name "*.temp" | xargs rm -f

Delete any unused duplicate asset files confirmed to have zero references in code
PROOF REQUIRED: Paste output of each command

STEP 3 — Firebase Version Lock to 11.6.0
bashcd artifacts/westcars
pnpm remove firebase
pnpm add firebase@11.6.0
pnpm list firebase

PROOF REQUIRED: Paste actual output of pnpm list firebase
Must show exactly firebase 11.6.0 — if anything else fix immediately

STEP 4 — Verify Firebase Config

Read the Firebase config file and paste full contents
Run:

bashgrep -rn "apiKey\|authDomain\|projectId\|storageBucket\|messagingSenderId\|appId" artifacts/westcars/lib --include="*.ts" --include="*.tsx"

Paste output — every value must use process.env.EXPO_PUBLIC_FIREBASE_* — zero hardcoded strings
Confirm single app initialization:

bashgrep -rn "initializeApp\|getApps" artifacts/westcars --include="*.ts" --include="*.tsx"

Must show getApps().length === 0 guard — never initialized twice
Confirm no firebase-admin in mobile code:

bashgrep -rn "firebase-admin" artifacts/westcars/app --include="*.ts" --include="*.tsx"

Must return zero results
PROOF REQUIRED: Paste all grep outputs

STEP 5 — Verify google-services.json
bashcat artifacts/westcars/google-services.json | grep "package_name"
grep "package" artifacts/westcars/app.json

Both must show exactly gh.westcars.app
If google-services.json is missing stop immediately and report — do not proceed
PROOF REQUIRED: Paste both outputs

STEP 6 — Fix Firebase Auth Platform Files
Completely rewrite both platform files:
lib/firebase-persistence.native.ts:
tsx// @ts-nocheck
import { initializeAuth, getReactNativePersistence } from 'firebase/auth';
import AsyncStorage from '@react-native-async-storage/async-storage';
import app from './firebase';

export const auth = initializeAuth(app, {
  persistence: getReactNativePersistence(AsyncStorage),
});
lib/firebase-persistence.ts:
tsximport { getAuth } from 'firebase/auth';
import app from './firebase';

export const auth = getAuth(app);

Confirm auth is imported from lib/firebase-persistence everywhere — never initialized twice:

bashgrep -rn "initializeAuth\|getAuth" artifacts/westcars/app --include="*.ts" --include="*.tsx"

Must return zero results — auth only initialized in lib/firebase-persistence
PROOF REQUIRED: Paste grep output and both file contents

STEP 7 — Completely Rewrite Login Screen
Delete app/auth/login.tsx entirely and write from scratch:
tsximport React, { useRef, useState } from 'react';
import {
  Text,
  TextInput,
  TouchableOpacity,
  ActivityIndicator,
  Platform,
  KeyboardAvoidingView,
  StyleSheet,
} from 'react-native';
import { ScrollView } from 'react-native-gesture-handler';
import { useRouter } from 'expo-router';
import { signInWithEmailAndPassword } from 'firebase/auth';
import { auth } from '../../lib/firebase-persistence';

export default function LoginScreen() {
  const router = useRouter();
  const passwordRef = useRef<TextInput>(null);
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState('');

  const handleLogin = async () => {
    setError('');
    if (!email.trim() || !password.trim()) {
      setError('Please fill in all fields');
      return;
    }
    try {
      setLoading(true);
      await signInWithEmailAndPassword(auth, email.trim(), password);
      router.replace('/(tabs)');
    } catch (e: any) {
      const msg = e.code === 'auth/invalid-credential'
        ? 'Incorrect email or password'
        : e.code === 'auth/too-many-requests'
        ? 'Too many attempts. Please try again later'
        : e.code === 'auth/user-not-found'
        ? 'No account found with this email'
        : e.code === 'auth/wrong-password'
        ? 'Incorrect password'
        : 'Login failed. Please try again';
      setError(msg);
    } finally {
      setLoading(false);
    }
  };

  return (
    <KeyboardAvoidingView
      style={styles.container}
      behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
    >
      <ScrollView
        contentContainerStyle={styles.scroll}
        keyboardShouldPersistTaps="always"
        showsVerticalScrollIndicator={false}
      >
        <Text style={styles.title}>Welcome Back</Text>
        <Text style={styles.subtitle}>Sign in to your Westcars account</Text>

        {error ? <Text style={styles.error}>{error}</Text> : null}

        <TextInput
          style={styles.input}
          placeholder="Email address"
          placeholderTextColor="#999"
          value={email}
          onChangeText={setEmail}
          keyboardType="email-address"
          autoCapitalize="none"
          autoCorrect={false}
          autoComplete="email"
          returnKeyType="next"
          blurOnSubmit={false}
          editable={!loading}
          onSubmitEditing={() => passwordRef.current?.focus()}
        />

        <TextInput
          ref={passwordRef}
          style={styles.input}
          placeholder="Password"
          placeholderTextColor="#999"
          value={password}
          onChangeText={setPassword}
          secureTextEntry
          autoCapitalize="none"
          autoCorrect={false}
          autoComplete="password"
          returnKeyType="done"
          editable={!loading}
          onSubmitEditing={handleLogin}
        />

        <TouchableOpacity
          style={[styles.button, loading && styles.buttonDisabled]}
          onPress={handleLogin}
          disabled={loading}
          activeOpacity={0.8}
        >
          {loading
            ? <ActivityIndicator color="#fff" />
            : <Text style={styles.buttonText}>Sign In</Text>
          }
        </TouchableOpacity>

        <TouchableOpacity
          onPress={() => router.push('/auth/signup')}
          activeOpacity={0.7}
          style={styles.link}
        >
          <Text style={styles.linkText}>
            Don't have an account?{' '}
            <Text style={styles.linkBold}>Create one</Text>
          </Text>
        </TouchableOpacity>

        <TouchableOpacity
          onPress={() => router.push('/auth/forgot-password')}
          activeOpacity={0.7}
          style={styles.link}
        >
          <Text style={styles.linkText}>Forgot password?</Text>
        </TouchableOpacity>

      </ScrollView>
    </KeyboardAvoidingView>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#0A1628' },
  scroll: { flexGrow: 1, padding: 24, justifyContent: 'center' },
  title: { fontSize: 28, fontWeight: '700', color: '#fff', marginBottom: 8 },
  subtitle: { fontSize: 16, color: '#aaa', marginBottom: 32 },
  input: {
    height: 52,
    backgroundColor: '#fff',
    borderRadius: 10,
    paddingHorizontal: 16,
    fontSize: 16,
    color: '#000',
    marginBottom: 16,
  },
  button: {
    height: 52,
    backgroundColor: '#00C897',
    borderRadius: 10,
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 8,
  },
  buttonDisabled: { opacity: 0.6 },
  buttonText: { color: '#fff', fontSize: 16, fontWeight: '700' },
  error: { color: '#ff4444', marginBottom: 16, fontSize: 14, textAlign: 'center' },
  link: { marginTop: 16, alignItems: 'center' },
  linkText: { color: '#aaa', fontSize: 14 },
  linkBold: { color: '#00C897', fontWeight: '700' },
});

PROOF REQUIRED: Read file back and paste first 20 lines

STEP 8 — Completely Rewrite Signup Screen
Delete app/auth/signup.tsx entirely and write from scratch:
tsximport React, { useRef, useState } from 'react';
import {
  Text,
  TextInput,
  TouchableOpacity,
  ActivityIndicator,
  Platform,
  KeyboardAvoidingView,
  StyleSheet,
} from 'react-native';
import { ScrollView } from 'react-native-gesture-handler';
import { useRouter } from 'expo-router';
import { createUserWithEmailAndPassword, updateProfile } from 'firebase/auth';
import { auth } from '../../lib/firebase-persistence';

export default function SignupScreen() {
  const router = useRouter();
  const emailRef = useRef<TextInput>(null);
  const passwordRef = useRef<TextInput>(null);
  const confirmRef = useRef<TextInput>(null);
  const [name, setName] = useState('');
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [confirm, setConfirm] = useState('');
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState('');

  const handleSignup = async () => {
    setError('');
    if (!name.trim() || !email.trim() || !password || !confirm) {
      setError('Please fill in all fields');
      return;
    }
    if (password.length < 6) {
      setError('Password must be at least 6 characters');
      return;
    }
    if (password !== confirm) {
      setError('Passwords do not match');
      return;
    }
    try {
      setLoading(true);
      const cred = await createUserWithEmailAndPassword(auth, email.trim(), password);
      await updateProfile(cred.user, { displayName: name.trim() });
      router.replace('/(tabs)');
    } catch (e: any) {
      const msg = e.code === 'auth/email-already-in-use'
        ? 'An account with this email already exists'
        : e.code === 'auth/invalid-email'
        ? 'Please enter a valid email address'
        : e.code === 'auth/weak-password'
        ? 'Password is too weak — use at least 6 characters'
        : 'Sign up failed. Please try again';
      setError(msg);
    } finally {
      setLoading(false);
    }
  };

  return (
    <KeyboardAvoidingView
      style={styles.container}
      behavior={Platform.OS === 'ios' ? 'padding' : 'height'}
    >
      <ScrollView
        contentContainerStyle={styles.scroll}
        keyboardShouldPersistTaps="always"
        showsVerticalScrollIndicator={false}
      >
        <Text style={styles.title}>Create Account</Text>
        <Text style={styles.subtitle}>Join Westcars today</Text>

        {error ? <Text style={styles.error}>{error}</Text> : null}

        <TextInput
          style={styles.input}
          placeholder="Full name"
          placeholderTextColor="#999"
          value={name}
          onChangeText={setName}
          autoCapitalize="words"
          autoCorrect={false}
          autoComplete="name"
          returnKeyType="next"
          blurOnSubmit={false}
          editable={!loading}
          onSubmitEditing={() => emailRef.current?.focus()}
        />

        <TextInput
          ref={emailRef}
          style={styles.input}
          placeholder="Email address"
          placeholderTextColor="#999"
          value={email}
          onChangeText={setEmail}
          keyboardType="email-address"
          autoCapitalize="none"
          autoCorrect={false}
          autoComplete="email"
          returnKeyType="next"
          blurOnSubmit={false}
          editable={!loading}
          onSubmitEditing={() => passwordRef.current?.focus()}
        />

        <TextInput
          ref={passwordRef}
          style={styles.input}
          placeholder="Password"
          placeholderTextColor="#999"
          value={password}
          onChangeText={setPassword}
          secureTextEntry
          autoCapitalize="none"
          autoCorrect={false}
          autoComplete="new-password"
          returnKeyType="next"
          blurOnSubmit={false}
          editable={!loading}
          onSubmitEditing={() => confirmRef.current?.focus()}
        />

        <TextInput
          ref={confirmRef}
          style={styles.input}
          placeholder="Confirm password"
          placeholderTextColor="#999"
          value={confirm}
          onChangeText={setConfirm}
          secureTextEntry
          autoCapitalize="none"
          autoCorrect={false}
          autoComplete="new-password"
          returnKeyType="done"
          editable={!loading}
          onSubmitEditing={handleSignup}
        />

        <TouchableOpacity
          style={[styles.button, loading && styles.buttonDisabled]}
          onPress={handleSignup}
          disabled={loading}
          activeOpacity={0.8}
        >
          {loading
            ? <ActivityIndicator color="#fff" />
            : <Text style={styles.buttonText}>Create Account</Text>
          }
        </TouchableOpacity>

        <TouchableOpacity
          onPress={() => router.push('/auth/login')}
          activeOpacity={0.7}
          style={styles.link}
        >
          <Text style={styles.linkText}>
            Already have an account?{' '}
            <Text style={styles.linkBold}>Sign in</Text>
          </Text>
        </TouchableOpacity>

      </ScrollView>
    </KeyboardAvoidingView>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#0A1628' },
  scroll: { flexGrow: 1, padding: 24, justifyContent: 'center' },
  title: { fontSize: 28, fontWeight: '700', color: '#fff', marginBottom: 8 },
  subtitle: { fontSize: 16, color: '#aaa', marginBottom: 32 },
  input: {
    height: 52,
    backgroundColor: '#fff',
    borderRadius: 10,
    paddingHorizontal: 16,
    fontSize: 16,
    color: '#000',
    marginBottom: 16,
  },
  button: {
    height: 52,
    backgroundColor: '#00C897',
    borderRadius: 10,
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 8,
  },
  buttonDisabled: { opacity: 0.6 },
  buttonText: { color: '#fff', fontSize: 16, fontWeight: '700' },
  error: { color: '#ff4444', marginBottom: 16, fontSize: 14, textAlign: 'center' },
  link: { marginTop: 16, alignItems: 'center' },
  linkText: { color: '#aaa', fontSize: 14 },
  linkBold: { color: '#00C897', fontWeight: '700' },
});

PROOF REQUIRED: Read file back and paste first 20 lines

STEP 9 — Verify Google Sign In

Read the Google Sign In file and paste full contents
Confirm WebBrowser.maybeCompleteAuthSession() at top of file
Confirm client IDs from env vars not hardcoded
Confirm signInWithCredential uses auth from lib/firebase-persistence
Check SHA-1:

basheas credentials -p android

Paste output confirming SHA-1 is listed
PROOF REQUIRED: Paste actual handler code

STEP 10 — Audit Every Screen and Feature
Read every file in app/ and confirm by pasting proof for each:
Navigation:

Every tab in (tabs) loads — read each file and confirm no crashes
Every router.push points to a real existing file
No screen renders blank from undefined data

Listings:

Listings load from Firestore — paste actual query code
Each listing card wrapped in TouchableOpacity — paste code
Listing detail loads all fields — paste screen code
Images have fallback — paste image component code

Seller Profile:

Phone number in card with numberOfLines={1} — paste code
ID verification centered with alignItems: 'center' — paste code
Call button uses Linking.openURL('tel:...') — paste code
Message button uses Linking.openURL('sms:...') — paste code

Sell/Post Listing:

All TextInputs have keyboard working pattern — paste code
Image picker working — paste code
Submits to Firestore — paste code

Messages:

Loads from Firestore — paste query
Message input keyboard works — paste code

Profile:

Logout clears session — paste code
Auth persists after restart — confirm via firebase-persistence.native.ts

STEP 11 — Audit All Cards for Broken onPress
bashgrep -rn "onPress" artifacts/westcars/app --include="*.tsx" | grep "<View"

Paste output — every View with onPress is broken — fix every one found
PROOF REQUIRED: Must return zero results after fixing

STEP 12 — Remove All Mockups and Placeholders
bashgrep -rn "TODO\|FIXME\|lorem\|dummy\|placeholder\|mock\|fake\|test@\|console\.log" artifacts/westcars/app --include="*.tsx" --include="*.ts"

Paste output before fixing
Fix or remove every result
Run again and paste output — must return zero results

STEP 13 — Icon Fonts Verification
Read app/_layout.tsx and paste the full useFonts call — confirm all families included:
bashgrep -rn "from '@expo/vector-icons'" artifacts/westcars/app --include="*.tsx"

Paste output — every family found must be in useFonts
PROOF REQUIRED: Paste useFonts block

STEP 14 — Splash Screen Verification
Read and paste full contents of app/splash.tsx — confirm:

useRouter() hook used — NOT static router import
2600ms timer with retry logic

Read and paste splash section of app/_layout.tsx — confirm:

SplashScreen.preventAutoHideAsync() at module level
5 second safety net exists
hideAsync() gated on fontsLoaded || fontError AND appReady
PROOF REQUIRED: Paste both code sections

STEP 15 — app.json Full Verification
Read and paste full app.json — confirm every field:

"owner": "lyon7sarbah" ✅
"newArchEnabled": true ✅
"softwareKeyboardLayoutMode": "pan" ✅
Icon ends in .png not .jpg ✅
react-native-keyboard-controller NOT in plugins ✅
android.package exactly gh.westcars.app ✅
ios.bundleIdentifier exactly gh.westcars.app ✅
projectId is e61e8d7b-541e-491c-afa1-219ca6f64d2d ✅

STEP 16 — Metro and Babel Verification
Read and paste metro.config.js — confirm:

platforms: ['android', 'ios', 'web', 'native']
watchFolders includes monorepo root
blockList excludes .git and .local

Read and paste babel.config.js — confirm:

react-native-reanimated/plugin NOT present
No transform-remove-console
No dead code elimination plugins

STEP 17 — TypeScript Final Check
bashcd artifacts/westcars && pnpm exec tsc --noEmit 2>&1

Paste full output
Fix every error found before proceeding
Must show zero errors
PROOF REQUIRED: Paste actual tsc output showing zero errors

STEP 18 — Final Clean and Production Build
Only run after ALL steps above pass with proof:
bashrm -rf .expo
rm -rf node_modules/.cache
rm -rf /tmp/metro-*
rm -rf /tmp/haste-*
cd artifacts/westcars && EAS_NO_VCS=1 eas build --platform android --profile production --non-interactive

Paste full build submission output including build URL

FINAL REPORT — Do NOT say READY FOR PLAY STORE unless every single item below has actual pasted proof:

✅ eas whoami output showing lyon7sarbah
✅ Zero davidsarb references anywhere — paste grep output
✅ app.json owner is lyon7sarbah — paste line
✅ pnpm list firebase showing 11.6.0
✅ Zero hardcoded Firebase keys — paste grep output
✅ google-services.json package name gh.westcars.app — paste grep output
✅ Login screen rewritten — paste first 20 lines
✅ Signup screen rewritten — paste first 20 lines
✅ Google Sign In verified — paste handler code
✅ Zero broken View onPress — paste grep output
✅ Zero mockups or console.logs — paste grep output
✅ All icon fonts in useFonts — paste block
✅ Splash screen using useRouter() hook — paste code
✅ app.json all fields verified — paste relevant lines
✅ TypeScript zero errors — paste tsc output
✅ EAS build URL — paste link
End with READY FOR PLAY STORE only if every item above has proof. Otherwise list exactly what still needs fixing with file name and line number.